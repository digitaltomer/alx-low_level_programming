#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: compare result
 */

int _strcmp(char *s1, char *s2)
{
	int flag;

	flag = strcmp(s1, s2);

	return (flag);
}
