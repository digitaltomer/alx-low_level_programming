#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch;

	putchar("lowercase characters : \n");
	for (ch = 'a'; ch <= 'z'; ch++)
{
	return (0);
}
