#include "main.h"

/**
 * int _abs(int) - computes the absolute value of an integer
 * @n: - int to check
 * Return: the abs of an integer
 */
int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
